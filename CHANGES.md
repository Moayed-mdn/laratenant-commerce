# سجل الإصلاحات — Justshop Merchant Dashboard

هذا الملف يوثّق الإصلاحات التي تمت على المشروع استجابةً لثلاثة طلبات: (١) إيجاد النصوص المكتوبة hardcode بالإنجليزية بدل الترجمة، (٢) تقسيم ملفات الترجمة الكبيرة، (٣) إصلاح تناسق الألوان.

---

## ١. تقسيم ملفات الترجمة

- `common.json` كان **٧٦-١٠٠ كيلوبايت** لكل لغة (يحتوي كل شيء: التنقل، تسجيل الدخول، الثيمات، الفوترة...). أصبح الآن **أقل من ٥٠٠ بايت**، ويحتوي فقط على الكلمات العامة المشتركة (حفظ، إلغاء، تحميل...).
- محتواه السابق أُخرج إلى **٢٠+ ملف مستقل** حسب الميزة: `nav.json`, `login.json`, `theme.json`, `billing.json`, `users.json`... إلخ.
- تم دمج `settings` و`cmsPages` بعمق داخل ملفاتهما المستقلة الموجودة أصلًا (كان بينهما تكرار ونصوص متضاربة تراكمت بمرور الوقت — تم توحيدها).
- **تحقّق آلي بالكامل**: كتبنا سكريبت يحاكي منطق تحميل الترجمات في `i18n.ts` ويقارن الناتج قبل/بعد — لا فرق في أي namespace لم يُغيَّر عمدًا.
- تطابق تام بين en/ar في جميع ملفات الترجمة (٣٥ ملفًا).
- تم تحديث كل الأماكن التي كانت تعتمد على المسار القديم داخل `common.json` (مثل `common.notFound` → `notFound`, و`common.theme.*` → `theme.*`).

### اكتُشف أثناء العمل
- منطق الدمج القديم كان يُسقِط بصمت بعض مفاتيح `cmsPages.sections` بسبب تعارض بين ملفين — تم حله بدمج عميق (deep merge) بدل الاستبدال الكامل.
- ملف `platform.json` كان يحتوي على مفتاحين (`platformDashboard`, `subscriptions`) بينما **كل الكود** (حوالي ١٥ ملفًا) يستخدمهما كـ namespace مستقل مباشرة — وهذا **لم يكن يعمل إطلاقًا** حتى قبل أي تعديل منا. تم إصلاحه بتقسيم الملف إلى `platformDashboard.json` و`subscriptions.json` (بدون تعديل ولا سطر كود واحد، لأن هذا هو بالضبط الشكل الذي كان الكود يتوقعه أصلًا).

---

## ٢. النصوص المكتوبة Hardcode بالإنجليزية

تم فحص المشروع بالكامل (٦٩٦ ملف) عبر أداة فحص مخصّصة (تلتقط نصوص JSX، الـattributes، رسائل toast، ورسائل confirm)، وتكرر الفحص عدة مرات للتأكد من عدم وجود إشكاليات جديدة بعد كل إصلاح.

**النتيجة: من ١٩١ موقعًا إلى صفر فعليًا** (تبقّى ١٤ حالة فقط، جميعها مقصودة عن وعي — انظر أدناه).

### أبرز ما تم إصلاحه
- **مشكلة RTL جذرية**: كان `<html>` الجذري يحمل `lang="en"` دائمًا وبدون `dir` إطلاقًا، بينما كان هناك متغيّر `dir` محسوب في مكان آخر لكن **لم يُستخدم أبدًا**. أي أن واجهة العربية لم تكن تحصل على اتجاه RTL ولا خط Noto Sans Arabic الصحيح. تم دمج الـlayout الجذري (`app/layout.tsx`) داخل `app/[locale]/layout.tsx` وربط `lang`/`dir` فعليًا.
- محرر أنظمة الألوان بالكامل (`ColorPicker`, `ColorSchemeCard`, `ColorSchemeEditorDialog`, `ColorSchemeManager`) كان غير مترجم ١٠٠٪.
- `SystemTemplateEditContent.tsx` (٤٥٢ سطرًا) لم يكن يستورد نظام الترجمة إطلاقًا.
- صفحات الأخطاء (`error.tsx`, `global-error.tsx`) كانت نصوصًا إنجليزية ثابتة رغم توفر الترجمة في أغلب الحالات (تُرك `global-error.tsx` بنص ثنائي اللغة مكتشَف من الرابط، لأنه تقنيًا يعمل خارج نطاق مزوّد الترجمة).
- جميع رسائل toast في الفوترة، الشحن، الملف الشخصي، القوالب — مع صياغة جمع عربية صحيحة (ICU plural) بدل "يوم/أيام" الإنجليزية.
- عدة مفاتيح ترجمة كانت **موجودة أصلًا لكن غير مربوطة بالكود** (orphaned) — تم استخدامها بدل تكرارها (مثال: `auth.loggingOut` كان موجودًا وجاهزًا).
- عناوين صفحات (`<title>`) كانت ثابتة بالإنجليزية، من ضمنها الجذر الكامل الذي كان لا يزال يحمل `"Create Next App"` (بقايا من إنشاء المشروع).
- تنسيق التاريخ/العملة كان يستخدم `'en-US'` ثابتًا في عدة أماكن — تم إصلاح الأداة المركزية (`src/lib/utils/date.ts`) لدعم `locale`، وربطها في الأماكن الأكثر ظهورًا (المدونة، التوثيق، اشتراكات المنصة).

### الحالات الـ١٤ المتبقية (متروكة عن قصد)
- أسماء اللغات في مبدّل اللغة (`English` / `العربية`) — تُعرض كل لغة باسمها الخاص، وهذا هو المعيار الصحيح عالميًا.
- أمثلة تقنية يجب أن تبقى حرفية: صيغة `index, follow` (SEO)، أمثلة hex مثل `#FFFFFF`، أمثلة روابط، أمثلة slugs.
- كود ميت غير مستخدم في أي مكان (`StorefrontHome.tsx`).

### ⚠️ متابعة مطلوبة (لم تُستكمل بالكامل)
دوال mapper في: `orders`, `products`, `categories`, `brands`, `tags`, `users`, `dashboard` (في `src/lib/mappers/`) لا تزال تستخدم `'en-US'` ثابتة عبر `src/lib/utils/date.ts` القديم. الأداة نفسها **أصبحت جاهزة** لدعم اللغة (راجع التوقيع الجديد)، لكن ربطها الكامل عبر hooks هذه الملفات (على غرار ما تم مع الاشتراكات) لم يتم لضخامة عدد نقاط الاستخدام. هذا يستحق جولة منفصلة.

---

## ٤. إصلاحات إضافية (أغسطس ٢٠٢٦)

### خطأ type-check في صفحة billing/plans
**الملف:** `src/app/[locale]/(merchant)/merchant/billing/plans/page.tsx`

**المشكلة:** كان هذا الملف الوحيد في المشروع الذي بقي على نمط Next.js القديم حيث كان `params` من نوع عادي `{ locale: string }` بدلاً من `Promise<{ locale: string }>` المطلوب في Next.js 15+.

**الإصلاح:**
```typescript
// قبل
export async function generateMetadata({ params }: { params: { locale: string } }) {
  const t = await getTranslations({ locale: params.locale, namespace: 'billing.plans' });

// بعد
export async function generateMetadata({ params }: { params: Promise<{ locale: string }> }) {
  const { locale } = await params;
  const t = await getTranslations({ locale, namespace: 'billing.plans' });
```

### نصوص hardcode في صفحات الـdescription
تم اكتشاف ٣ حالات إضافية من النصوص المكتوبة hardcode لم يلتقطها الفحص السابق لأنها كانت في `description` prop مخصّص وليس attribute HTML قياسي:

**١. shipping/page.tsx**
- استبدال: `"Manage shipping methods, zones, and address validation."` → `{t('subtitle')}`
- المفتاح كان موجودًا مسبقًا في `shipping.json` لكن غير مستخدم

**٢. orders/page.tsx**
- استبدال: `"View and manage customer orders."` → `{to('subtitle')}`
- تم إضافة `useTranslations('orders')` باسم `to`
- المفتاح كان موجودًا مسبقًا في `orders.json`

**٣. categories/page.tsx**
- استبدال: `"Organise your products with categories."` → `{ct('subtitle')}`
- المفتاح كان موجودًا مسبقًا في `categories.json`
- **إصلاح إضافي:** كان `useTranslations('categories')` يُستدعى **بعد** return شرطي، مما يخالف React Hooks Rules (قد يسبب خطأ "Rendered fewer hooks than expected"). تم نقله إلى أعلى الدالة قبل أي return.

### التحقق
- ✅ `npm run type-check` يعمل بدون أخطاء
- ✅ جميع مفاتيح الترجمة المستخدمة موجودة في ملفات `en/` و `ar/`
- ✅ تطابق تام بين اللغتين في جميع المفاتيح المستخدمة

---

## ٣. تناسق الألوان

### التشخيص (بالأرقام، معيار WCAG)
ألوان success/warning/danger/info كانت تُستخدم كنص (badges) بنفس القيمة في الوضعين النهاري والليلي. النتيجة: تباين **١.٩٨–٣.٨:١** في النهاري (فاشل — الحد الأدنى المطلوب ٤.٥:١) مقابل **٤.٧–٨.٤:١** في الليلي (جيد). هذا يطابق تمامًا الوصف: "غير مريحة، خصوصًا في النهاري".

### الإصلاح
- أُعيدت معايرة قيم الإضاءة (lightness) في الوضع النهاري فقط عبر `src/app/globals.css`، مع الحفاظ الكامل على قيم الليلي عبر تجاوزات `.dark` صريحة (تم التحقق رقميًا من كل الأزواج بعد التعديل — جميعها الآن ≥٤.٨:١).
- كذلك أُصلحت `--color-subtle` (كانت ٢.٤٤:١) و`--color-muted` و`--color-border-strong` (كانت شبه غير مرئية عند ١.٤٩:١، أصبحت ٣.١:١).
- ربط ألوان إشعارات Toast (Sonner) بنظام الثيم الفعلي للتطبيق: المشروع يستخدم Zustand (`useUiStore`) لإدارة الوضع الليلي/النهاري فعليًا، بينما كان مكوّن Toaster يقرأ من مكتبة `next-themes` **المثبَّتة لكن غير المفعَّلة في أي مكان بالتطبيق** (لا يوجد `<ThemeProvider>` منها إطلاقًا) — أي أن الإشعارات كانت دائمًا تتبع تفضيل نظام التشغيل بدل اختيار المستخدم الفعلي داخل التطبيق.
- تحويل **٢٤٥ استخدامًا** لألوان Tailwind خام (`amber-900`, `green-600`, `red-600`...) عبر ٣٩ ملفًا إلى نظام الألوان الموحّد (`text-warning`, `bg-danger-bg`...). تم فحص كل حالة يدويًا لتحديد إن كانت فعلًا تمثّل حالة (نجاح/تحذير/خطأ/معلومة) أو كانت استخدامًا مقصودًا (كألوان "نجوم التقييم" الذهبية التقليدية، أو نقاط شريط المتصفح الزخرفية، أو تظليل كتل الأكواد) — وتُركت هذه الحالات كما هي عن قصد.
- إصلاح جانبي: مكوّن `Tabs` كان يستخدم رمادي خام (`gray-800`) للتبويب النشط في الوضع الليلي بدل رمز التطبيق الفعلي، ومكوّن `PopoverDescription` كان يجعل **كل** نص وصف بكل popover في التطبيق أحمر بشكل ثابت (على الأرجح خطأ غير مقصود) — تم إصلاح كليهما.

---

## ملاحظة حول التحقق

تعذّر تشغيل `npm install` / `tsc --noEmit` في هذه البيئة (حظر على مستوى الشبكة). للتعويض، تم بناء أداتين مخصّصتين للتحقق:
1. فاحص نصوص hardcode (بُني تدريجيًا وحُسِّن مرتين لتقليل الأخطاء الإيجابية الكاذبة).
2. فاحص "وجود مفتاح الترجمة" — يحاكي دمج الترجمات الحقيقي في `i18n.ts`، ويتتبّع كل استدعاء `t()`/`useTranslations()` في المشروع ويتحقق من وجود المفتاح فعليًا في en/ar. هذا الفحص هو ما كشف خلل `platform.json` ومشاكل أخرى موصوفة أعلاه.

يُنصح بتشغيل `npm run type-check` و`npm run build` محليًا للتأكد النهائي قبل النشر، نظرًا لعدم القدرة على تشغيلهما هنا.
