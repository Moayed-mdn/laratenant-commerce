# Archive

This folder tracks documentation that is no longer canonical.

Archived material includes:

- temporary fix reports
- phase completion logs
- duplicate copies
- outdated auth/migration assumptions

Canonical guidance now lives in `architecture/`, `standards/`, `features/`, `security/`, and `decisions/`.
