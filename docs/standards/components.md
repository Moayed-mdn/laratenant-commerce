# Component Standards

- keep route pages thin; move behavior to `_components`.
- isolate feature components by domain (users/products/orders/dashboard).
- shared components belong in `components/shared`.
- UI primitives belong in `components/ui`.
- avoid business logic in presentational UI components.
