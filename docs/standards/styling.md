# Styling Standards

- Tailwind utility classes are default styling approach.
- design tokens are centralized in `src/app/globals.css`.
- use existing UI primitives before creating one-off controls.
- support dark mode and RTL where applicable.
- avoid hardcoded colors when a token/semantic class exists.
